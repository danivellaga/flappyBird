import { View } from 'react-native';

const Bird = () => {
  return (
    <View
      style={{
        position: 'absolute',
        backgroundColor: 'blue',
        width: 50,
        height: 50,
        left: 50,
        bottom: 100,
      }}
    />
  );
};

export default Bird;